{
  "name": "json-server",
  "version": "0.0.1",
  "description": "Pequeño servidor con informacion para practicar y tener como guia",
  "main": "app.js",
  "scripts": {
    "start": "json-server --watch db.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tomas Aquino",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
